/**
 * 2つの数値を足し算する関数
 * @param a 1つ目の数値
 * @param b 2つ目の数値
 * @returns 足し算の結果
 */
export function add(a: number, b: number): number {
  return a + b;
}

// 使用例
const result = add(5, 3);
console.log(`5 + 3 = ${result}`);
